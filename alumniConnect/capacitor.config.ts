import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nyp.alumniConnect',
  appName: 'alumniConnect',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
